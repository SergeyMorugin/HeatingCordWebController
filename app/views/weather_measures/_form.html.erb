<%= form_with(model: weather_measure, local: true) do |form| %>
  <% if weather_measure.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(weather_measure.errors.count, "error") %> prohibited this weather_measure from being saved:</h2>

      <ul>
      <% weather_measure.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :temperature %>
    <%= form.text_field :temperature %>
  </div>

  <div class="field">
    <%= form.label :pressure %>
    <%= form.text_field :pressure %>
  </div>

  <div class="field">
    <%= form.label :humidity %>
    <%= form.text_field :humidity %>
  </div>

  <div class="field">
    <%= form.label :rainfall %>
    <%= form.text_field :rainfall %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
